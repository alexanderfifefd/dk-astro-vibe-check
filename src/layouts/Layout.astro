---
interface Props {
  title: string;
  description?: string;
}

import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const { title, description = "Datakollektivet — Et datafellesskap for alle" } = Astro.props;
---

<!doctype html>
<html lang="nb">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title} | Datakollektivet</title>
  </head>
  <body class="min-h-screen flex flex-col bg-base text-text">
    <Header />

    <main class="flex-1">
      <slot />
    </main>

    <Footer />
  </body>
</html>

<style is:global>
  @import "../styles/global.css";
</style>
